{
    "rules": {
      ".read": "auth != null",
      ".write": "auth != null",
      "messages": {
        "$messageId": {
          ".read": "auth != null",
          ".write": "auth != null && auth.uid == newData.child('uid').val()"
        }
      }
    }
  }